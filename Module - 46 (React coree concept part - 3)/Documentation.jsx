{ /*

1 . e.target.[input field name].value
2 . use form action and formData in the action handler .  formData.get("input field name")
3 . controlled components . one per each field use state on change of the field . useful to dynamic error hamdling

4 . Uncontrolled form wwhere we are gonna use the ref hooks
*/}
